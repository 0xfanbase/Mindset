# v4.0 audit — a statement, not a chart (2026-09-22)

Round: the owner called the v3.0 page boring and asked for all eight of the director's UI
suggestions plus a single dark theme, with explicit future-proofing as weeks fill in. Fable
wrote the spec and reviewed; one Opus agent implemented code, verifier and docs; one fresh-context
Opus auditor ran report-only against commit 198d9f4. Every fix below was applied by the director
and re-verified (67/67, nine browser scenarios clean).

## Independently reproduced
- `verify.mjs all` 67/67 three times; two lib.mjs mutations (fraction forced to 0, milestone sort
  dropped) each failed loudly and were reverted to a clean tree. Check-name ledger re-derived by
  diffing the old and new name sets: 12 out, 13 in, 66 -> 67.
- JS 41,395 B of 65 KB; page 57,571 B of 600 KB; Appendix C.2 byte-identical to sw.js including
  ASSETS; CLAUDE.md 18 lines.
- Future-proofing sweep, every day 2026-09-22 to 2091-01-01 for J and B (46,956 assertions, 0
  failures): weeksLived monotone, +1 exactly every 7 days until the clamp (J full 2079-08-11, B
  2078-07-12); weekProgress fraction exactly (d+1)/7 and resetting on each increment;
  upcomingMilestones never throws, sorted, weeksUntil >= 0, empty once clamped; displayWeek never
  exceeds 4,680. View math at the clamp reasoned and confirmed in-browser: decade window starts at
  4,160 (gutter 80..89), year at 4,628, no marker, no out-of-range index.
- Browser, nine scenarios (390x844 today, 320, desktop, reduced motion, 2030, 2050, 2066, 2079,
  2090): zero console errors, zero overflow; hero complete above the fold (bars bottom 452 at
  390x844); switch, arrow keys, three views and the resume day-flip behave. Extra scenario: three
  consecutive pinned days in Year view, canvas pixels read at the now cell: 13/31, 18/31, 22/31
  filled against ideals 0.43, 0.57, 0.71; reproduced with no reload via visibilitychange.
- Accessibility: every rendered text pair >= 4.5:1 (tightest 6.09); tap targets >= 44px; roving
  tabindex and arrow keys on the tablist and radiogroup; focus rings visible; reduced motion
  disables the halo, bar transition and reveal. PII: initials and birth months only. Zero
  quotation-mark glyphs in any rendered string.

## Findings and dispositions (all nits, no blockers)
1. Decade view tinted the whole panel (the window is the current decade). Tint now Life-only.
2. Tabs lacked aria-controls and the panel aria-labelledby. Added; panel named by the active tab.
3. Four nested nodes named "Life in weeks". The tabpanel's redundant label removed.
4. Canvas label kept the 13-month head-start sentence past the clamp. Dropped when both are full.
5. build-log wording said 8 retargets including the merge; corrected. The verifier's tablist
   count now strips comments first. BUILD-PLAN §2 item 7's `nav` landmark mention is pre-existing
   and left alone.
