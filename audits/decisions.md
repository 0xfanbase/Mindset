# Decisions (ADR-lite)

- **2026-07-13, Stage 0 — branch/deploy target:** owner explicitly authorized building directly on `main` for this repo (no feature-branch/PR flow) so GitHub Pages serving and cron scheduling work without an extra merge step.
- **2026-07-13, Stage 0 — PII invariant scope:** owner clarified invariant 1 protects the owner and his wife specifically; public-figure attribution (Seneca, Bill Perkins, Dweck, Carnegie, etc.) is intentional and required, not a violation.
- **2026-07-13, Stage 0 — signature element:** replaced the v1.0 "particle brain" with a water-drop animation, matching a further-along Claude Design prototype (project `84cffca3-021f-4e25-a4d7-f9a9a4b209d7`) the owner had already built. Source files (`mindset-drop.js`, `mindset-lib.js`, `mindset-data.js`) pulled via DesignSync and adapted into `drop.js`, `lib.mjs`, and the Stage 3 content seed.
- **2026-07-13, Stage 0 — Pages enablement gate:** this environment has no GitHub-MCP tool for reading/enabling Pages settings, and direct GitHub REST/token calls are policy-blocked (the session's own operating rules require MCP tools only, enforced by an auto-mode classifier). Cannot confirm or flip the Pages toggle programmatically. Asked the human to confirm/enable Pages manually (Settings → Pages → Deploy from a branch → `main` / root). Proceeding with the rest of the build in parallel since this is a one-time settings toggle with no reason to expect it can't succeed; live-URL checks in Stage 4/5 will confirm once it's on.

- **2026-07-13, Stage 1 — contrast fix:** darkened blossom `--accent` from the design prototype's `#C94F7C` (~4.30:1 on white, fails AA 4.5:1 for normal-size text) to `#B84870` (~4.99:1 on `--surface`, ~4.61:1 on `--bg`) — invariant 7 requires 4.5:1 for any pair used at normal text size, and `--accent` renders the active-tab underline text, inline links, and the "Read →" label, all body-size.

- **2026-07-13, Stage 4 — feed resolution (5/5 verified):**
  - Daily Stoic `https://dailystoic.com/feed/` — 200, title "Daily Stoic", 10 items.
  - Farnam Street `https://fs.blog/feed/` — 200, title "Farnam Street", 20 items.
  - Huberman Lab `https://feeds.megaphone.fm/hubermanlab` — 200, title "Huberman Lab", 422 items.
  - The Mindset Mentor (Rob Dial) — resolved via Apple Podcasts Search API (`itunes.apple.com/search?media=podcast&term=...`) to `https://feeds.simplecast.com/rpKQEwel`; verified title "The Mindset Mentor", `itunes:author` "Rob Dial", 1899 items.
  - Ali Abdaal — resolved via the canonical `<link rel="canonical">` / `og:url` on `youtube.com/@aliabdaal` (NOT the first bare `"channelId"` string match, which pointed to three different, unrelated channel IDs elsewhere in the page — confirms the exact risk the amended plan flagged) to channel ID `UCoOae5nYA7VqaXzerajD0lg`; feed verified, title "Ali Abdaal", `<name>Ali Abdaal</name>`, 15 entries.
  - All 5/5 sources verified live + identity-checked. No escalation needed (§11.3 only fires at zero verified).

- **2026-07-13, Stage 5 prep — icon pipeline (Linux):** no rsvg-convert/ImageMagick/Inkscape/cairosvg available and npm install is banned, so used the pre-installed headless Chromium binary (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless --screenshot=...`) as the rasterizer — a one-shot CLI invocation, not a dev server. Produced icon-512.png, icon-192.png, apple-touch-icon.png (180px) from favicon.svg (a minimal single-drop mark replacing v1.0's node-network brain mark), ~16KB total, well under the 150KB icon budget.

- **2026-07-13, Stage 4 — idempotency bug found and fixed:** the first local run of `generate-daily.mjs` picked "Ali Abdaal"; a same-day re-run (simulating the plan's own local-run-then-workflow-run sequence) flipped the pick to "The Mindset Mentor". Root cause: the "recently used sources" window was computed from the full history including *today's own just-upserted entry*, so a re-run treated its own prior pick as something to avoid. Fixed by excluding the current `dateHKT`'s entry when computing recent-source preference. Verified stable across 3 consecutive same-day re-runs after the fix.

- **2026-07-13, Stage 4 — Pages confirmed NOT enabled (concrete, not hypothetical):** dispatched `watchdog` against the real fresh `daily.json` and it correctly failed + filed https://github.com/0xfanbase/Mindset/issues/1, because the live Pages URL is unreachable. Direct check confirms: `curl https://0xfanbase.github.io/Mindset/` → 404 "Site not found · GitHub Pages". This is the Stage 0 Pages-gate concern materializing for real. No GitHub MCP tool manages Pages settings, and direct REST/token calls are policy-blocked in this environment, so this genuinely requires the repo owner to enable it manually (Settings → Pages → Source: Deploy from a branch → `main` / `/(root)` → Save). Proceeding with the rest of Stage 5 (everything not requiring a live URL); holding the `v1.0` tag and the live-URL checks until Pages is confirmed live.

- **2026-07-13, Stage 5 — Pages root-caused and resolved:** confirmed via `has_pages: true` (API) that the owner's setting change registered, but the live URL kept 404ing even after a fresh push, with zero `page_build`/deployment activity visible anywhere in the repo. Diagnosis: the repo's Pages source is set to **"GitHub Actions"**, not "Deploy from a branch" — that mode needs an actual `actions/deploy-pages` workflow to ever publish anything; without one, Pages sits enabled but permanently empty, which is exactly what was observed. Added `.github/workflows/pages-deploy.yml` (checkout + `configure-pages` + `upload-pages-artifact` + `deploy-pages`, triggered on push to `main` and `workflow_dispatch`). It ran successfully on the next push and the site went live immediately (`/`, `/data/daily.json`, `/manifest.webmanifest`, `/sw.js` all confirmed 200 with correct content). Re-dispatched `watchdog` afterward — now passes clean. Closed issue #1.

- **2026-07-13, Stage 5 — cannot push the v1.0 tag:** `git push origin v1.0` (and the explicit refspec form) both fail with HTTP 403, while every branch push in this entire build succeeded normally through the same proxy — this looks like a structural restriction on tag refs specifically, not a transient error (tried twice, same failure). No GitHub MCP tool creates tags or releases either (checked `get_tag`/`get_release_by_tag`/`list_releases` — all read-only). The tag exists locally (`git tag -a v1.0`) but isn't pushed. Everything else in the acceptance checklist is genuinely complete and live; this is a cosmetic/bookkeeping gap, not a functional one — the site works fully without a pushed tag. Flagged to the repo owner: either push it yourself (`git push origin v1.0` from a machine with real GitHub credentials) or create a GitHub Release for `v1.0` off the `main` branch via the web UI, which creates the tag as a side effect.

- **2026-07-13, v1.2 — signature element replaced (drop → yoga figure):** live human feedback: "change water droplet to a small human doing yoga sun salutation on repeat." Replaced `drop.js`/`<mindset-drop>` with an original `figure.js`/`<mindset-figure>` implementation — an 11-pose loop (standing prayer → raised arms → forward fold → lunge → plank → up-dog → down-dog → lunge → fold → raised arms → standing), side-profile stick figure, same offscreen-sprite glow technique, same invariants (pause on hidden, static under reduced motion, DPR-capped, no shadowBlur, <=12KB).
- **2026-07-13, v1.2 — root-caused and fixed "wasn't moving":** the drop's `_setup()` silently returned if the element had zero size at connect-time (e.g. if the stylesheet was still loading), relying solely on `ResizeObserver`'s own delayed initial callback to ever recover. `figure.js`'s `_setup()` now retries via `requestAnimationFrame` until it gets a real size, so a slow-loading stylesheet can no longer leave the animation permanently stuck on a static first frame. Also made the pose-to-pose transitions themselves clearly visible motion, rather than relying on one subtle physical effect the way the drop did.
- **2026-07-13, v1.2 — Today tab decluttered:** live feedback: "the today tab looks clustered." Loosened card padding (18px→20px), inter-card gap (11px→16px), body line-height (1.5→1.55); tightened header padding (14px→10px top) to offset the taller figure box (84px→112px, needed for the new figure's floor poses).
- **2026-07-13, v1.2 — values cut from 10 to 5:** live feedback: "condense values to five top ones... 10 is too many." Kept the 5 originally marked "core" in the source content (Consistency, Ownership, Patience, Focus, Presence); dropped the 5 always marked "reserve, held back for later curation" — they were never promoted, so this isn't a new deletion of shipped content, just declining to promote them further.
- **2026-07-13, v1.2 — mindset-relevance filter on the Fresh pipeline:** live feedback: an Ali Abdaal video about AI-prompt tactics appeared as the Fresh card, off-theme for a mindset app. Root cause: 4 of 5 sources are single-topic by nature (always on-theme); Ali Abdaal's channel is topically broad. Added `OFF_THEME_PATTERNS` (AI-tool/prompt tactics, app/tech reviews, sponsorship language, pure-entertainment recommendations) to `generate-daily.mjs`, and upgraded the parser to check up to 5 items per feed (newest-first, take the first that passes both the 14-day-window and on-theme filters) rather than only ever looking at the single newest item — this avoids dropping a topically-broad source entirely just because its newest post happens to be off-theme. Regenerating the live `daily.json` with the fix: the AI-prompts video and a "harry potter fan fiction" video were both correctly filtered, landing on a genuinely on-theme pick ("Unlearn Negative Thoughts And Behavior Patterns," The Mindset Mentor). This is a keyword heuristic, not a semantic classifier — logged as a known limitation with the honest fallback (remove the source if it recurs) documented in the README.
- **2026-07-13, v1.2 — sw.js cache version bump:** `CACHE` bumped `mindset-v1` → `mindset-v2` since the asset list changed (`drop.js` → `figure.js`) — old clients' stale caches get purged via the existing `activate` handler rather than serving a mix of old and new cached files.

- **2026-07-13, v1.2 follow-up — "animation still not moving" (anti-stuck pivot, §9.2):** live feedback reported the figure is still static after the drop→figure fix. Tried four distinct approaches to get a real-browser read on the deployed site and none produced a usable result, each failing a different way: (1) headless Chromium `--virtual-time-budget` screenshot diff — invalidated by a control test proving this flag does not drive `requestAnimationFrame` at all in this Chromium build; (2) CDP-over-WebSocket without an explicit proxy flag — Chrome silently failed to navigate (`document.URL` stuck at `about:blank`) because Chrome does not read `HTTPS_PROXY` on Linux; (3) same, with `--proxy-server` set — persistent TLS handshake resets (`net_error -101`/`-202`), most likely Chrome negotiating HTTP/2 through a proxy tunnel that (per `/root/.ccr/README.md`) does not support HTTP/2 tunneling; (4) same, with `--disable-http2` added — the Chrome process didn't start at all (no log file written, exit 144, not an OOM per `free -h`). Per invariant/§9.2, four failures of the same underlying approach (headless-browser automation in this sandbox) is a mandatory pivot: stopped pursuing live-browser verification and fell back to static evidence instead —
  - `git status`/`git log`: `dff3d53` (the fix) is on `origin/main`, working tree clean, nothing unpushed.
  - Live `curl` of the deployed site: `index.html` correctly contains `<mindset-figure id="figure">` and `<script type="module" src="./figure.js">`; live `figure.js` is **byte-identical (md5 match)** to the local fixed file and served fresh (`last-modified` matches the v1.2 deploy, `cache-control: max-age=600`, not a stale/simulated response).
  - `sw.js` fetch handler is network-first (`fetch(...).then(res => { if (res.ok) cache.put... ; return res })`, cache is only a `.catch` fallback) with `skipWaiting()`/`clients.claim()` on install/activate — a stuck stale-cache serving an old `drop.js`/blank frame indefinitely is unlikely given this strategy.
  - Manual trace of `figure.js`'s init path (`connectedCallback` → `_setup` retry-via-rAF → `_refresh` → `_start` → `_step` rAF loop) found no logic bug; `_animated` intentionally returns `false` (static `STANDING` frame only) when `prefers-reduced-motion: reduce` matches, which is invariant-7-required accessibility behavior, not a defect.
  - Conclusion: the deployed code is correct and live. The most likely explanation for a persistently static figure that code-side verification cannot rule in/out is the viewer's own OS-level "Reduce Motion" accessibility setting (by design) or a not-yet-updated PWA home-screen icon needing one reload for the new service worker to take control. Flagged to the human to confirm device/setting rather than guessing further and risking removing or weakening the reduced-motion accessibility behavior.

- **2026-07-13, v1.3 — `voices` anchor category added:** live feedback: the household finds Jay Shetty, Joe Biden, and Michelle Obama personally inspiring and asked whether their thinking could surface in the app. Rather than a new subsystem, added 9 anchor cards (3 each) to the existing `anchors` pool under a new `voices` category — they now surface through the same deterministic daily rotation as everything else, roughly 1 day in 14 given the new 129-card pool. `CATEGORY_COUNTS`/total-anchors assertions in `verify.mjs` extended (120 → 129) and BUILD-PLAN.md §5.2/§5.3 updated accordingly — this is a spec extension, not a relaxation of any existing check, per invariant 12.
  - Given two of the three are former holders of significant political office, applied a higher bar than the standard six categories (now codified as BUILD-PLAN.md §5.3.9): strictly personal-character/resilience/grief/self-belief content, zero reference to office/party/policy/elections; attribution tied to a specific nonpartisan book (`— after X, from <book>`) rather than their public role; closeness-to-source enforced against the *spirit* of "no verbatim quotes," not just the quote-glyph scan.
  - Ran an independent adversarial review pass (a separate agent, not the author) against all 9 drafts before merging, per §5.3.8's process. It flagged one card (the original voices-006) as a generic resilience platitude wearing Joe Biden's name rather than something distinctly traceable to his public record, and flagged two others (voices-007, voices-009) as sitting close to specific branded phrasing (the Obama Foundation/Becoming tagline "Your story is your power," and the Reach Higher initiative, respectively) even though both passed the mechanical checks. All three were rewritten before merging; see `audits/CONTENT-REVIEW.md`'s new "voices" section for the full before/after reasoning per card.
  - Mechanically verified clean: all 9 within the 40-word cap (24-35 words each), zero quotation-mark glyphs, zero banned platitudes, unique ids, no near-duplicate (>0.6 token-overlap) against any of the other 129 anchors.

- **2026-07-13, v1.4 — reduce-motion diagnosis confirmed:** the human confirmed turning off the phone's Reduce Motion accessibility toggle fixed the "not moving" report, with no code change — validates the v1.3 anti-stuck conclusion. No further action needed on this item.

- **2026-07-13, v1.4 — figure moved to the top of the page:** live feedback: "I want it to sit at the top of the page." Moved `.figure-wrap` out of `<main>` to be `<body>`'s first child, ahead of `<header>`, rather than removing the wordmark/theme-toggle row entirely — the toggle is a real accessibility control (theme switching) and the wordmark is the only branding on the page, so reordering achieves "figure at the top" without losing either. Safe-area-inset-top padding moved from `header` to `.figure-wrap` accordingly; header's own top padding dropped to a small fixed value since it's no longer adjacent to the notch/status bar. Total vertical space consumed is unchanged (still one no-scroll screen, invariant 11) — only the order changed.

- **2026-07-13, v1.4 — animation redesigned for realism and pace:** live feedback: "much better" (re: the figure existing at all) but asked for the movement itself to look "realistic" and "slow," with a reference chart of the classical 12-count Surya Namaskar A sequence attached. Two changes: (1) **timing** — replaced the old constant-speed model (12 poses interpolated at one even rate, full loop in 12s) with a hold-then-ease model: each pose is actually held for a beat (600ms–1.9s, longest at down-dog and the standing poses, matching how those are actually held longest in practice, shortest at the brief eight-limbed bridge pose) before a separate eased transition (0.9–1.3s) into the next pose. Full cycle is now 26.2s. Verified by simulating the segment-lookup logic in Node against the reference-chart timing model before touching the browser: confirmed the cycle length and that all 11 segments are reached in order across a full sweep. (2) **pose accuracy** — added the eight-limbed pose (Ashtanga Namaskara, the low bridge between plank and up-dog) that the reference chart includes and the shipped v1.2/v1.3 sequence was missing; now 12 named poses instead of 11. (3) **rendering** — added a filled torso band (shoulder-to-hip, perpendicular-offset quad) instead of a single wire-thin spine line, so the figure reads more like a body; verified via a Node script that every pose's torso vector is non-degenerate (shoulder and hip are never coincident) and all joint coordinates stay within the normalized 0–100 box. `figure.js` grew from ~8KB to ~10KB, still under the 12KB budget (`verify.mjs` confirms). `sw.js` `CACHE` bumped `mindset-v2` → `mindset-v3` (BUILD-PLAN.md Appendix C.2 amended to match) since core assets changed again.

- **2026-07-13, v1.5 — Fable-led audit: daily-refresh pipeline was silently broken (headline finding):** requested audit of UI/UX, code, and whether the daily pipeline works in practice, plus a full repo PII sweep. Full detail in `audits/v1.5-fable-audit.md`; summary here. Live-dispatched `daily.yml` twice back to back: anchor/shift/fresh picks were byte-identical across the two runs (only the timestamp differed) — genuine same-day idempotency confirmed in production. But neither resulting bot commit triggered `pages-deploy.yml` at all (checked its run history directly — zero runs for either commit): GitHub does not fire other workflows' `on: push` for commits authored with the default `GITHUB_TOKEN`, a documented loop-prevention behavior nobody had hit yet because every prior push this session was authored by the session's own git identity, not the bot's token. Left unfixed, the live site would never pick up any future day's cron-generated update, and `watchdog.yml`'s existing check (calendar-date comparison only) could not have caught it, since the date string doesn't change within the same day. Fixed: `daily.yml` now runs `gh workflow run pages-deploy.yml` right after a successful commit (needs `permissions: actions: write`, added); `watchdog.yml` now also compares `generatedAtISO` between live and committed, safe from false alarms since it runs 3h after the expected refresh (well past the observed 600s CDN cache lag). This fix is mechanically verified (YAML valid, `verify.mjs all` 59/59) but not yet live-tested against a merged `main` — dispatching a workflow always runs the version already committed to the target ref, so this genuinely can't be proven until after merge. Flagged in the PR: dispatch `daily.yml` once post-merge and confirm `pages-deploy.yml` fires for the resulting commit.
- **2026-07-13, v1.5 — audit process note (models + fact-checking):** dimension reviews (UI/UX, code quality) ran on a cheaper non-Fable model per the request's token-efficiency instruction; Fable synthesized the final verdict as project director. Every finding from the cheaper model was independently re-verified before acting — this caught the cheaper model's WCAG contrast math being wrong on three pairs (recomputed relative luminance by hand in Node: 5.05:1/4.79:1/4.61:1, all pass 4.5:1, contradicting the review) and a false "no cursor:pointer on tabs" claim (already present) — both discarded rather than acted on. Two real findings did survive: no `:focus-visible` anywhere in `styles.css` (fixed: added `--accent`-colored focus outlines on the theme toggle, tabs, and Fresh card link) and `.card`'s `box-shadow` hardcoded to blossom's ink RGB, giving calm-theme cards a faint pink tint (fixed: added a per-theme `--shadow` custom property). Also fixed one real code gap: `app.js`'s `renderValues()`/`renderToday()` sat outside `boot()`'s `try`/`catch` — a fetch *failure* was already handled correctly (traced `staleness(null)` -> `"offline"` -> safe fallback, confirmed no bug), but malformed *data* (not just a failed fetch) could have thrown uncaught there instead of showing the graceful error card; moved both calls inside the `try` block.
- **2026-07-13, v1.5 — post-merge live verification (the deploy-chain fix confirmed working):** PR #2 merged. Per the PR's own test-plan item that couldn't be run pre-merge, dispatched `daily.yml` against `main` immediately after merge: it committed `d95bf15` and — this time — `pages-deploy.yml` shows a real `workflow_dispatch`-triggered run for that exact commit (run `29229072331`, success), proving the self-dispatch fix works in production, not just on paper. Confirmed the live site's `data/daily.json` is now byte-identical to the committed file, including `generatedAtISO` — the deploy genuinely landed, no CDN-cache-lag ambiguity. Also dispatched the tightened `watchdog.yml` against this state: passes clean (run `29229114598`), confirming the new `generatedAtISO` comparison doesn't false-positive against a healthy, fully-deployed state. This closes the one open item from the audit; no further action needed.

- **2026-07-13, v1.5 — PII/full-history scan (clean):** `git rev-list --all` + `git grep` across every commit reachable from any branch/ref for the one real name relevant here, the owner's known email, generic email/phone patterns, and PII-adjacent phrases, plus every commit's author/committer identity and every filename ever committed. All commit identities are pseudonymous/service accounts (GitHub-noreply, AI-assistant-noreply, github-actions[bot]) — no real name or personal email in git metadata, ever. Zero email addresses found in any commit content. The only numeric "phone-number-like" hits were the xmur3/mulberry32 PRNG constants (confirmed via context, not real numbers). The only "PII-adjacent phrase" hits were the project's own written PII policy text appearing in every historical revision of BUILD-PLAN.md, describing the rule, not violating it. No unexpected files or local/upload paths were ever committed. Conclusion: no PII linking to the owner or his wife anywhere in current content or full history.

- **2026-07-13, v1.6 — header combined into one line:** live feedback with an annotated screenshot: wordmark, date, and toggle should align in one line (they were split across two rows — wordmark+toggle in the header, date on its own line in `main`). Moved `#date-line` into `<header>` and switched `header` from a 2-item flex row to a 3-column CSS grid (`1fr auto 1fr`) — a plain flex `space-between` with 3 children wouldn't truly center the date, since the wordmark and toggle have different widths; the grid guarantees the date sits in the true visual center regardless. `.chip`'s top margin adjusted since it's now `main`'s first child instead of following the date line.

- **2026-07-13, v1.6 — figure poses rebuilt with fixed anatomical bone lengths (real bug, not a style complaint):** live feedback asked to audit whether the movement was really a sun salutation "and not random moves," in line with human anatomy. Measured every pose's segment lengths (neck, torso, upper arm, forearm, front/back thigh, front/back shin) with a script: they were wildly inconsistent — torso ranged 8.6 to 34.1 units depending on the pose, forearm 3.6 to 30.3, back shin 15.5 to 37.1 — meaning the figure's limbs were effectively stretching and shrinking between poses, a real anatomical bug from hand-placing each pose's coordinates independently rather than from a shared skeleton. Rebuilt all 8 named poses via forward kinematics: one fixed bone-length skeleton (neck 12, torso 27, upper arm 17, forearm 20, thigh 21, shin 21 — identical everywhere, verified by script after the rebuild), with a joint angle per pose chosen from the actual Surya Namaskar A biomechanics (e.g. down-dog's hip is the single highest point forming the inverted-V peak; up-dog's shoulder arches well above the hip in the backbend; the eight-limbed bridge pose has the chest notably below the hip while the hip stays raised) and sanity-checked numerically — hand/foot positions land where they should relative to the hip and shoulder for that specific pose (e.g. in the forward fold, the hand now reaches almost exactly to the floor near the feet; previously, several poses' hands were 20-30 units short of or past where they needed to be). Chose a slightly generous but *fixed* arm length (37 total vs. a stricter ~29) since the true anatomical length couldn't reach the floor from a reasonably-composed shoulder height in several floor poses — a deliberate, consistent stylization choice, not another inconsistency.
- **2026-07-13, v1.6 — figure rendered as a solid silhouette, not a stick figure:** live feedback, with a yoga reference photo attached, asked for "an actual human, not a stickman." Read this as "make the body read as solid mass," not "embed this exact copyrighted stock photo" — literally copying a third-party reference image would also conflict with the project's vector/theme-color-reactive design (the figure needs to recolor live on theme toggle, which a static raster photo can't do) and raises a licensing question for content that isn't ours. Rewrote `_draw()`: every limb is now a filled tapered quad (wider at the body-side joint, narrower at the extremity, plus a rounded cap at each end) instead of a thin stroked line — one fill per segment, still cheap to draw every frame. `figure.js` grew from ~10KB to ~11KB, still under the 12KB budget.
- **2026-07-13, v1.6 — visual verification method:** the live deployed URL couldn't be used for a real browser test per the standing v1.3 anti-stuck note (headless Chrome can't reliably navigate it in this sandbox). Instead served the repo locally (`http://127.0.0.1:8791`, a one-off Node `http` server, no network/proxy involved) and screenshotted it with the pre-installed headless Chromium. Hit a genuine, reproducible tooling limitation along the way: the one-shot `--screenshot` CLI mode ignores `--window-size` entirely for the actual CSS layout viewport — confirmed via a diagnostic page printing `window.innerWidth`, which read 500 regardless of `--window-size`, `--headless=new`, or even a hardcoded `<meta name="viewport" content="width=390">`. Rather than keep fighting a tooling limitation (already pivoted off headless-Chrome-based live testing once this session per the anti-stuck rule), screenshotted at the actual 500px width Chrome insists on instead of a cropped 390px — confirmed the header now shows wordmark/date/toggle correctly on one line, and the figure renders as a solid colored silhouette rather than a stick figure. Did not attempt to visually verify each of the 8 individual poses in motion (the same `--virtual-time-budget` limitation from the v1.3 investigation still applies — it doesn't drive `requestAnimationFrame`); relied on the numeric pose audit above for those instead.

- **2026-07-13, v1.7 (part 1 of 2) — header restructured to fill both top corners, date moved under the wordmark, card gap widened:** live feedback with two annotated screenshots: the figure sat alone in a wide empty top strip with the wordmark/date/toggle in a separate row below it, wasting the top-left/top-right corners; and the stacked cards read as touching rather than clearly separate. Restructured `<header>` into a single row containing `.brand` (wordmark + a smaller date line stacked left-aligned underneath it, moved out of the centered position it had in v1.6) on the left, `.figure-wrap` centered, and the theme toggle on the right, all in one 3-column grid — this fills the corners the figure used to leave empty and pulls the wordmark/toggle up to the true top of the page, per the request. `.panel`'s card gap widened 16px → 24px for clearer separation. Verified with the same local-server-plus-screenshot method as v1.6 (confirmed working at the tool's fixed 500px render width) — wordmark+date now sit top-left, figure centered, toggle top-right, cards visibly separated.

- **2026-07-13, v1.7 (part 2 of 2) — animation rebuilt ground-up after "defying gravity" feedback, Fable-led:** live feedback: the movement looked like it defied gravity and needed to actually match real yoga practice; asked to use Fable for a full rebuild plan. Briefed Fable with the exact current architecture (9-joint model, the just-fixed bone lengths, per-pose hip positions chosen independently for "nice composition") and asked it to research real Surya Namaskar A mechanics and diagnose the specific failure. Fable's diagnosis, which I independently confirmed by reading the actual v1.6 pose coordinates: (1) the figure silently flipped which direction it faced twice per cycle (some poses had the head at small x, others at large x); (2) the planted hand "teleported" between six different x-positions across poses where a real hand stays put on the mat; (3) plank's feet computed 33 units *above* its own hands (legs pointing into the sky); (4) down-dog's hip apex (y=30) was geometrically taller than the fixed arm+leg reach could actually support from the floor. Root cause common to all four: each pose's hip position had been chosen freely per pose for visual balance rather than derived from a shared ground reference.
  - **Fix:** rebuilt all 8 poses via inverse kinematics from one fixed floor line (y=92) and one fixed hand-plant spot (x=84, unmoving from the forward fold through down-dog — matching how a real practitioner's hands stay on the mat) instead of composing each pose's hip freely. Wrote a 2-bone IK solver (law of cosines) so a shoulder reaching to a grounded hand target lands exactly there given the fixed arm length, and a "hip from foot" solver that works backward through a chosen leg angle so a grounded foot lands exactly at its target. A single consistent facing direction is used throughout (no more flips).
  - **A real bug caught mid-rebuild, not just diagnosed at the end:** the first pass at the lunge pose put the hip too far from the hand target for the fixed-length arm to reach — caught by an explicit reachability check (`shoulder-to-hand distance <= arm length`) added to the build script *before* trusting any output, not after. Also caught and fixed a plain JS bug of my own (object-destructuring `{kf, ff}` on a function that returns an array `[kf, ff]`, silently producing `undefined` joints) via the same script's assertions rather than a silent bad render.
  - **Verified three separate ways, all before touching the live site:** (1) the build script asserts every pose's 8 bone-segment lengths match the fixed skeleton and every hand-reach distance is within the arm's actual length; (2) after transcribing the output into `figure.js`, re-extracted the pose objects straight out of the shipped file (not from my own script's memory) and re-ran the same checks independently — confirmed byte-for-byte consistent; (3) local HTTP server + headless screenshot, confirming the figure renders in a sane position with no crash.
  - **Replaced up-dog with cobra (Bhujangasana)** per Fable's recommendation — up-dog's old geometry didn't hold together on its own (head and feet ended up on the same side of the hip, which no real backbend does), and cobra is calmer/simpler, matching the "calm and peaceful and mindful" framing of the request.
  - **Timing rebalanced** per Fable's research into real vinyasa pacing: a real sun salutation's movement lasts most of the breath, and only down-dog (traditionally five breaths) and the standing poses are actually held for any length. Replaced the old hold-heavy model (0.6–1.9s holds, ~1s snaps) with slower transitions (2.2–3.1s) and brief settles (0.5–0.7s, down-dog 5s) — full cycle ~42s, up from 26.2s.
  - **Added a small procedural foot-lift** (a sine arc applied only to whichever foot is mid-step) on the sequence's four stepping transitions, so a foot doesn't visibly drag along the floor between poses — Fable flagged this as a lower-priority (P2) refinement; included since it was cheap to add and directly serves the "calm, real movement" goal.
  - `figure.js` grew to ~11.6KB (was ~11KB), still under the 12KB budget. `verify.mjs all` 59/59.

- **2026-07-13, v1.7 (part 3 of 3) — Fable holistic ("insightful, calming, mindful") content audit, findings actioned:** live feedback asked for a Fable-led audit of the whole two-tab site's content and tone, separate from the animation rebuild. Gathered a representative sample of real card text across all eight anchor categories plus shifts/values first, so Fable was judging actual shipped content rather than guessing at it. Fable's core finding: most of the library holds up, but a handful of cards use hustle/output-flavored language under a calm surface — a "substance filter" distinguishing content whose payoff is a state of being (what this app is for) from content whose payoff is an output (a KPI in disguise). Five specific items were named or matched Fable's named pattern closely enough to act on:
  - `focus-001`: "guard three uninterrupted hours the way you guard revenue" reframed depth as a revenue-protection tactic. Reworded to anchor the comparison to something restorative instead: "Depth is rare, so depth is valuable. Protect three uninterrupted hours the way you protect sleep -- non-negotiable, not optional."
  - `rel-001`: "interest given first returns multiplied" read as relationships-as-investment-return. Reworded to state the human payoff directly: "Ask about their work before mentioning yours. Genuine curiosity about someone else is rare enough that people remember who offered it."
  - `wealth-001`: "paranoia" is a clinical/anxious word for what Housel's own framing calls caution. Reworded "paranoia" -> "caution", and "know which game today is" -> "know which game today calls for" for a cleaner sentence.
  - `wealth-008`: the savings-rate-vs-salary framing was a numerical outbuilding contest, not reflective. Reworded to a values-revealing frame: "What you choose not to spend says more about your priorities than what you earn ever will."
  - `values.json`'s Ownership `behaviour`: "Sends the follow up unprompted; no one has to chase" was output/throughput-flavored (a task-management metric, not a value in action). Reworded to a concrete, still-mundane act that reads as care rather than responsiveness: "Fixes the small broken thing instead of just mentioning it."
  - `shift-002`'s `to` side: "Finishing one thing that matters" nearly duplicated shift-001's own resolution in spirit. Reworded to "Choosing what earns a reply" -- keeps the inbox-clearing frame but names a judgment being exercised, not a productivity outcome.
  - All five drafts were pre-validated against `verify.mjs`'s exact mechanical rules (word count, quote-glyph scan, banned-platitude scan, reimplemented in a throwaway Node script) before being written into `data/cards.json`/`data/values.json`, then the full suite re-run after: `verify.mjs all` 59/59, unchanged from before the edit (word caps, ids, and category counts are all unaffected by a same-field reword).
  - Fable's other concrete recommendation was a visual one, not a content one: the Fresh card (third card, third-party linked content) was visually identical in weight to the Anchor/Shift cards it sits below, competing with them rather than reading as a lighter-touch invitation. Gave `.card-fresh` (applied to both the live-fetched Fresh card and its offline Reserve-shelf fallback) a flat, bordered, shadowless treatment against the app's page background instead of the elevated white/shadowed treatment the other two cards use, muted its chip and link color to `--muted`, and reworded its link text from the imperative "Read →" to the softer "Worth a look →". Fresh was already the last of the three cards in render order (`paintCards([anchor, shift, fresh])`), so "never above the fold" was already satisfied and needed no change.
  - Verified visually with Playwright + the pre-installed headless Chromium at a real 390×844 mobile viewport (this tooling combination renders at the actual requested viewport size, unlike the one-shot `--screenshot` CLI path used in v1.6/v1.7-part-1, which is fixed at ~500px regardless of flags) — confirmed the header layout from part 1, the widened card gaps, the Fresh card's quieter flat treatment against the other two cards' shadowed treatment, and the updated Ownership value text, all render correctly with no console errors on both the Today and Values tabs.

- **2026-07-13, v1.8 — yoga figure replaced with a glowing bottle of light:** live feedback with two annotated screenshots asked to (a) replace the yoga figure with a slowly-glowing bottle of light for a more Zen feel, (b) reduce the empty space between the header and the Today/Values tab line, (c) make the Fresh card match the style of the other two Today cards, and (d) make all three Today cards visually identical to the Values tab. Went through each one by one:
  - **Bottle of light:** rewrote `figure.js` from scratch, keeping the exact same public surface (`<mindset-figure>` tag, `color`/`glow`/`animate` attributes, the same lifecycle scaffold — zero-size-retry `_setup`, `ResizeObserver`-debounced re-setup, `visibilitychange` pause, `prefers-reduced-motion` static frame, offscreen-sprite glow with no per-frame `shadowBlur`) so `app.js`'s theme-toggle wiring needed no changes at all. Replaced the entire pose/inverse-kinematics system with: a stroked glass-bottle path (canvas path, low-opacity outline in the theme color), a light source inside it that brightens and dims on a continuous 7-second sine wave (no held frames, no snaps — a genuine "very slow" breathing glow, not a pose held-then-eased), and two or three small embers drifting upward inside the glass on an independent 9-second loop, fading in near the base and out near the neck. `figure.js` dropped from ~11.6KB to ~7.6KB. `verify.mjs` stage2 initially failed on the "no shadowBlur anywhere" check — not a real shadowBlur call, but a code comment that used the word "shadowBlur" as a negative reference ("no per-frame ... shadowBlur cost"); reworded the comment rather than weakening the check, since the check is correctly banning the literal API, not commentary about it.
  - **Header-to-tabs spacing:** shrank `mindset-figure`'s CSS box from 84×96 to 56×64 (a bottle icon doesn't need the vertical room a standing human figure did) and trimmed `header`'s bottom padding (6px → 2px) and `.tabs`'s top margin (4px → 0) to close the gap the screenshot circled.
  - **Fresh card / Values-tab visual match — and a real dormant bug found along the way:** while unifying the three Today cards with each other and with the Values tab, checked why the existing `.panel { gap: 24px }` (added in v1.7 part 1 specifically to separate the cards) didn't already make them look separated in the current screenshot. Inspected the live DOM with Playwright (`getBoundingClientRect` on each `.card`): card 1's bottom pixel was exactly equal to card 2's top pixel, and card 2's bottom equalled card 3's top — the three cards had **zero space between them**, touching edge-to-edge. Root cause: `.panel`'s `gap` arranges `.panel`'s own direct children, but `.panel`'s only direct child is the `#cards` wrapper div — the actual `.card` elements are `#cards`'s children, one level too deep for that `gap` to ever reach them. The v1.7-part-1 change had shipped, verified 59/59, and been genuinely believed to work, but never visibly did anything.
  - **Fix:** rather than re-patch `gap` onto the correct element, unified `.card` with `.value-row`'s existing flat/hairline-divided styling (`padding: 15px 0`, `border-bottom: 1px solid var(--hairline)`, no background/shadow/radius, no border on the last card) — this both closes the dormant spacing bug (each card now visibly separates itself via its own border, independent of any parent `gap`) and makes Today and Values read as one consistent list style, per the "identical look and feel" request. Retired the v1.7-part-3 `.card-fresh` quiet/bordered treatment since every card now already uses that same quiet flat style by default; kept its softened "Worth a look →" copy. Swapped the old `.card:hover` lift (which fired on non-interactive Anchor/Shift cards too) for a hover state scoped to `a.card-link` only, since Fresh is the sole actually-clickable card — matching how the non-interactive Values rows have no hover state at all.
  - **Found and fixed the same root-cause bug a second place, unprompted:** the desktop (`≥900px`) 3-across card layout had the identical mistake — `#panel-today { flex-direction: row }` targeted `.panel`, not `#cards`, so it never actually arranged the cards side by side either. Fixed it the same way (moved the flex/gap rules onto `#cards`, dropped the border-bottom on desktop cards since a horizontal gap replaces it there) and confirmed via a 1100px-wide screenshot that the three cards now genuinely lay out in a row.
  - **BUILD-PLAN.md** updated throughout: title bumped to v1.8, new changelog entry, §1/§3/§4.1/§4.4/§4.5/§4.6/§4.7/§10/§12/§13 rewritten wherever they described the yoga figure or the old card treatment as current truth (historical v1.1–v1.7 changelog entries were left untouched, since they're an accurate record of what was true at the time, not a live spec). `README.md`'s one-line description updated too.
  - **Verified:** `verify.mjs all` 59/59; visually confirmed via Playwright + headless Chromium at a real 390×844 viewport across four states — Today tab (two frames ~2.5s apart, confirming the glow actually brightens over time), Values tab, blossom theme, `prefers-reduced-motion: reduce` (renders a static mid-glow bottle, not blank) — plus a 1100px desktop-width screenshot confirming the newly-fixed 3-across layout. Zero console errors in any state.

- **2026-07-13, v1.9 — visible entrance animation + Today's cards restored (reversing part of v1.8):** live feedback, in one message: (a) wants a noticeable animation on page load, giving "opening up of the cards" as the example; (b) wants Today's items to read as actual cards again so the tab feels easy to read, mindful, and like something to learn from; (c) doesn't like the Fresh card and asked to see alternative ideas (not implemented yet — see the separate ideas note below, pending the human's pick).
  - **Entrance animation:** the existing `cardIn` keyframe (a 250ms, 8px fade/rise) was real but subtle enough that a live viewer didn't register it as an animation at all. Redesigned it as a 500ms fade + 14px rise + scale-from-0.97 with an eased curve (`cubic-bezier(0.16,1,0.3,1)`, a gentle overshoot-free ease-out), and increased the per-item stagger (Today's cards: 60ms → 90ms; added the same 60ms stagger to the Values tab's rows, which previously had no entrance animation or stagger at all) so the cards visibly cascade in one after another — literally "opening up" — rather than fading in as one block. Added a `prefers-reduced-motion` guard (`.card, .value-row { animation: none; }`) so this is fully suppressed for anyone with that OS setting, matching how the figure already behaves.
  - **Today's cards restored as actual cards, deliberately un-unifying from Values:** v1.8 had flattened Today's cards to exactly match the Values tab's hairline-divided row style, per that round's explicit "identical look and feel" request. This round's feedback reverses that specifically for Today: brought back a real card container (`--surface` background, 20px radius, `0 10px 28px var(--shadow)`) with breathing room between cards. Since v1.8 already diagnosed that a `.panel`-level `gap` never reaches the actual `.card` elements (they're `#cards`'s children, not `.panel`'s), the reintroduced spacing was applied directly to `#cards` (`display:flex; flex-direction:column; gap:14px`) rather than repeating that mistake a third time. The Values tab's own flat/hairline style is deliberately left untouched — Today and Values are now intentionally different (cards to read and learn from vs. a quiet reference list), not identical; this supersedes v1.8's card-unification framing in §4.5.3. Updated the desktop (`≥900px`) 3-across media query to match (gap-based row spacing, dropped the now-irrelevant `border-bottom: none` override).
  - **Verified:** `verify.mjs all` 59/59; visually confirmed via Playwright at a real 390×844 viewport — a screenshot taken immediately on load shows only the Anchor card visible/animating in with Shift and Fresh not yet appeared (proving the stagger is real, not just present in code), a settled-state screenshot of both tabs showing Today's boxed/shadowed cards clearly distinct from Values' flat rows, zero console errors.
  - **Fresh card:** not touched this round. The human asked to see alternatives before any implementation — ideas presented separately, pending their choice before any code or pipeline change (replacing Fresh could mean touching `generate-daily.mjs`'s feed-fetching, `daily.json`'s schema, `watchdog.yml`'s freshness check, and `cards.json`'s `freshReserve` pool, so getting direction first avoids throwing away pipeline work).

- **2026-07-13, v1.10 — Fresh card retired, replaced with Word of the Day; the whole RSS pipeline goes with it:** presented four concrete alternatives (Micro-Practice, Reflection Prompt, Word of the Day, or simply dropping the third card) plus a fifth prose-only option (curate the existing feed idea rather than replace it); the human picked **Word of the Day**. Treated this as more than a content swap: since the new third card is self-authored and deterministic (same rotation mechanism as anchor/shift, not a network fetch), the entire RSS/YouTube-fetching subsystem built up since v1.0 no longer has anything to do, and leaving it in place would be dead code contradicting the project's own "no half-finished implementations, delete what's unused" standard.
  - **Content:** authored 30 entries (`data/cards.json`'s new `wordOfDay` array, replacing `freshReserve`'s 10) — `{ id, word, origin, meaning }`, e.g. `wabi-sabi`/Japanese/"Finding beauty in what is imperfect, weathered, and incomplete." Pre-validated all 30 against `verify.mjs`'s exact word-cap (new: ≤20w for `meaning`)/quote-glyph/platitude rules via a throwaway Node script before writing, same method used for prior content edits this session. `origin` is always a language/tradition, never a person's name, since a word's origin doesn't carry the same attribution-confidence risk a paraphrased idea does.
  - **Pipeline removal:** `scripts/generate-daily.mjs` rewritten from ~180 lines (feed fetching, XML/CDATA parsing, off-theme-pattern filtering, recent-source dedup, `fresh-history.json` idempotency) down to ~35 (compute `dateHKT`/`dayNumber`, pick `anchorId`/`shiftId`/`wordId` via the same `pickIndex` used for anchors/shifts, write `daily.json`) — no more `fetch`, no more `AbortController`, no async at all. Deleted `state/fresh-history.json` and, since nothing else ever lived there, the now-empty `state/` directory along with it (git doesn't track empty directories, so this also means a fresh clone genuinely won't have a `state/` folder — updated `verify.mjs`'s stage0 directory-existence check and `BUILD-PLAN.md`'s §3 tree to match, rather than leaving a stale requirement for a directory nothing populates).
  - **`daily.json` schema simplified:** `fresh: {title,url,source,publishedISO} | null` → `wordId: string` (always present, never null — there's no fetch to fail). `.github/workflows/daily.yml`'s commit step no longer stages `state/fresh-history.json` (deleted).
  - **`app.js` simplified:** removed `validFreshUrl`, `domainOf`, `renderFreshCard`, `renderReserveCard`, and the `freshOk` branch between the last two — replaced with one `renderWordCard(word)` used identically whether the pick came from `daily.json` or the offline `pickToday()` fallback (previously Fresh and its reserve fallback were two different render paths with different content; now there's exactly one path, since Word of the Day can't fail to resolve the way an external URL could). Net removal, not net-neutral: fewer functions, fewer branches, after this change.
  - **`styles.css` cleanup:** removed `.fresh-footer`/`.fresh-domain`/`.fresh-read`/`.fresh-reserve-label` (no longer referenced) and every `a.card-link` rule (`:focus-visible`, `:hover`, `:active`, the base `display:block` rule) — confirmed via grep that nothing in `index.html`/`app.js` creates an `<a>` element anymore now that Fresh (the only link-shaped card) is gone. Added one new class, `.word-title` (Fraunces, 20px), for the word itself as a small headline between the card's chip and its meaning — otherwise reuses `.card-chip`/`.card-body`/`.card-attr` unchanged.
  - **`verify.mjs` updated, check count drops 59→58 (logged per the invariant-12 ratchet rule):** stage3's shape/count/word-cap/quote-glyph/platitude checks retargeted from `freshReserve`/`r.text` to `wordOfDay`/`w.meaning`; stage4's `daily.json` schema check now asserts `wordId` is a string instead of validating-or-nulling a `fresh` object; the `state/fresh-history.json` upsert-by-date check is **removed outright** (not loosened) since the file and the behavior it tested no longer exist — this is a legitimate deletion of a check whose subject was deleted, not a relaxation of a still-applicable rule.
  - **BUILD-PLAN.md, a large forward-spec pass (not just a changelog entry):** title bumped to v1.10; §1/§3/§4.3/§4.4/§4.5/§5.1–5.3/§6.1/§6.2/§7/§8/§9.2/§10 (Stage 3 + Stage 4 task/DoD/Verify text)/§11 all updated wherever they described the Fresh card, the RSS pipeline, or `freshReserve` as current truth; historical v1.0–v1.9 changelog paragraphs left untouched as an accurate record of what was true at each point in time. §7 rewritten in full (was ~40 lines of feed-resolution/identity-check/relevance-filter spec, now describes the deterministic three-pick stamp). §11's escalation triggers renumbered from four to three — "Feed collapse" (zero of five sources verifiable) has nothing left to apply to now that there's no feed; every "the four named triggers" cross-reference updated to "three" **except** one deliberately-preserved historical parenthetical in §9.2 rule 5 describing what v1.0's original (contradictory) document said, which still accurately said "four" at the time.
  - **`audits/CONTENT-REVIEW.md`:** replaced the "Fresh reserve (offline fallback)" section with a new "Word of the Day" section listing all 30 entries; added a v1.10 addendum note alongside the existing v1.3/v1.7 ones. Also fixed an unrelated staleness bug spotted while in this file: the "Values" summary section's Ownership row still showed the pre-v1.7-part-3 behaviour text ("Sends the follow up unprompted...") instead of the actually-shipped "Fixes the small broken thing instead of just mentioning it." — that content edit had updated `data/values.json` and the inline per-value entry earlier in this same file, but missed this second summary listing; corrected now.
  - **Verified:** `verify.mjs all` 58/58; ran the rewritten `generate-daily.mjs` for real (not just `node --check`) and confirmed it produces a schema-correct `daily.json` (`word-16` = Gemütlichkeit for today's date); visually confirmed via Playwright at a real 390×844 viewport that the Word card renders correctly (chip, word title, meaning, origin) with zero console errors, styled identically to Anchor/Shift.

- **2026-07-13, v1.11 — real header-gap bug found and fixed; Word of the Day gets a pronunciation button:** two requests: (a) tighten the space between the bottle and the Today/Values line, again — this had already been adjusted twice (v1.8's header/tabs padding trim, and implicitly by every round since), yet a visible gap remained; (b) add a speaker icon to the Word card that pronounces the word correctly when tapped.
  - **The real cause of the persistent gap:** rather than shave more padding blindly a third time, inspected the live DOM with Playwright (`getComputedStyle` + `getBoundingClientRect`). Found `#staleness-chip` carries `class="chip"` at all times — including in the default "fresh" (no-chip) case, where only its `hidden` attribute is set, never its class. `.chip { display: table; ...}` is an **author-stylesheet** rule; the browser's own default `[hidden] { display: none }` rule is **user-agent-stylesheet** in origin. Per the CSS cascade, origin is resolved *before* specificity — author-normal rules always beat user-agent-normal rules at the same specificity, regardless of which selector "looks" more specific or which rule appears later. So `.chip`'s `display: table` silently won every time, and the "hidden" chip was never actually hidden — it sat there empty, still consuming its `padding: 5px 12px` + `margin: 10px auto 0` box (~22px total), on every single page load in the common case. Confirmed via `getComputedStyle(chip).display` returning `"table"` even with `chip.hidden === true`. The exact same codebase already has the correct defensive pattern one line away (`.panel[hidden] { display: none; }`) — `.chip` just never got the equivalent rule, almost certainly an oversight from when the chip's styling was first written, not a deliberate choice.
  - **Fix:** added `.chip[hidden] { display: none; }`. Re-measured: the figure-to-tabs gap dropped from 24px to 2px, with zero additional padding/margin changes needed anywhere else. This is a real, previously-undiagnosed bug, not a design/taste adjustment — worth calling out since three prior rounds (v1.7 part 1, v1.8, v1.9) each nudged header/tabs spacing incrementally without ever finding this, because none of them checked computed `display` against the `hidden` attribute directly.
  - **Word of the Day pronunciation:** added a 44×44px `<button aria-label="Pronounce <word>">🔊</button>` in a new `.word-title-row` next to `.word-title`, calling `window.speechSynthesis.speak(new SpeechSynthesisUtterance(word.word))` with `utterance.lang` set per-entry (native browser Web Speech API — no library, no network call, same category as `fetch`/`ResizeObserver` already used elsewhere in this zero-dependency app). Rendered only when `"speechSynthesis" in window`, so unsupported browsers simply never see the button rather than seeing a dead one.
  - **Added a `lang` field to all 30 `wordOfDay` entries**, mapped from `origin` to a BCP-47 tag so the browser actually attempts the word in a matching voice/locale rather than defaulting to whatever the page's default voice is (which would otherwise read foreign words in an English accent/phonetic guess). Mapping used: Japanese→`ja-JP`, Latin→`la`, Nguni→`zu-ZA` (Ubuntu is most commonly associated with Zulu/Xhosa; picked Zulu), Danish→`da-DK`, Finnish→`fi-FI`, Greek/Stoic Greek→`el-GR`, Portuguese→`pt-PT`, German→`de-DE`, Swedish→`sv-SE`, Chinese/Confucian Chinese→`zh-CN`, Sanskrit→`hi-IN` (closest widely-supported voice; no common browser TTS locale exists for Sanskrit specifically), Italian→`it-IT`, Spanish→`es-ES`, Hebrew→`he-IL`. Applied via a one-off Node script (not by hand) to guarantee every entry got a mapping and to avoid transcription slips; the script also re-verified the resulting JSON parses and diffed cleanly (only additive `lang` lines, no incidental reformatting of the rest of `cards.json`).
  - **`verify.mjs`:** added a new stage3 check asserting every `wordOfDay` entry has non-empty `word`/`origin`/`lang`/`meaning` strings and that `lang` matches a BCP-47-shaped pattern (`/^[a-z]{2,3}(-[A-Z]{2})?$/`) — this is a genuine new requirement (not a re-add of the removed `fresh-history.json` check), so the count goes 58 → 59, still consistent with the invariant-12 ratchet (checks may tighten freely; only loosening needs a logged decision, and this is a tightening).
  - **Verified:** `verify.mjs all` 59/59. Confirmed the gap fix via `getBoundingClientRect` before/after (24px → 2px) and a settled screenshot. Confirmed the pronunciation feature functionally — since headless Chromium's real `SpeechSynthesis.speak()` does native type-checking on its argument (a naive full-object mock throws `Failed to execute 'speak' ... parameter 1 is not of type 'SpeechSynthesisUtterance'`), verified by wrapping only `speechSynthesis.speak` itself (leaving the real `SpeechSynthesisUtterance` constructor untouched) and asserting the intercepted call received the exact expected `{ text: "Gemütlichkeit", lang: "de-DE" }` after a real click — not just that the button exists. The rendered speaker glyph shows as a placeholder box in this sandbox's headless-Chromium screenshot because that environment lacks a color-emoji font; this is a known environment limitation (real iOS/Android/desktop browsers render 🔊 natively) and not a rendering bug in the app itself.

- **2026-07-13, v1.12 — Shift card retired, replaced with a mindful Journal prompt:** live feedback: "the current version seems to be not so helpful," referring to the Shift card's terse "From X → To Y" reframe (present unchanged since v1.0). Authored 40 open-ended reflection prompts (`journal-01` through `journal-40`) — second-person questions meant to actually be sat with (e.g. "What's one thing you're avoiding right now, and what is it costing you to keep avoiding it?"), specific and concrete rather than generic ("How are you feeling today?" was deliberately avoided as a category, along with anything a yes/no answer could dispatch). Pre-validated all 40 against `verify.mjs`'s exact quote-glyph/platitude rules and a new word cap (raised to ≤25 words from Shift's 8-word half-reframe cap, since a real question needs the room) before writing, via the same throwaway-script method used for every other content batch this session.
  - **Same architecture, new content type:** Journal rotates deterministically exactly like Anchor/Word of the Day always have (`pickIndex` keyed by a `"journal"` salt) — this is a pure content-pool rename/retarget (`shifts` → `journal`, `{from,to}` → `{prompt}`), not a new subsystem. Touched: `data/cards.json` (`shifts` array replaced), `lib.mjs`'s `pickToday`, `scripts/generate-daily.mjs` (`shiftId` → `journalId`), `data/daily.json`'s schema, `app.js` (`renderShiftCard`'s custom strikethrough-`from`/bold-arrow-`to` markup replaced by a much simpler `renderJournalCard` — just the `JOURNAL` chip plus the prompt in the existing `.card-body`, no new CSS needed), `styles.css` (removed the now-dead `.shift-from`/`.shift-to`/`.shift-to .arrow` rules), and every corresponding `verify.mjs` stage3/stage4 check (shape, count, word cap, quote-glyph, platitude scan, offline-fallback simulation, `daily.json` schema).
  - **`audits/CONTENT-REVIEW.md`:** kept the full "Shifts" section (all 40 entries plus their real editorial history — duplicate-cognitive-move catches, two near-verbatim-quote risks found and reworded) as an explicitly-labeled historical record rather than deleting it, since that content documents genuine QA work even though the pool no longer ships; added a new "Journal prompts" section with the same pre-validation note pattern used for Word of the Day.
  - **BUILD-PLAN.md:** bumped to v1.12, changelog entry added; §1/§3/§4.3/§4.4/§4.5/§5.1–5.3/§6.1/§6.2/§7/§9.3/§10 (Stage 4 task/verify text) updated wherever they described Shift/`shifts`/`shiftId` as current truth; historical changelog paragraphs and the Stage-3-seed-content mentions of the original design prototype's "10 shifts" left untouched as accurate history of what was authored/named at the time.
  - **Verified:** `verify.mjs all` 59/59 (same count as before — this is a like-for-like retarget of existing checks, not a net add or removal). Ran the real `generate-daily.mjs` and confirmed a schema-correct `daily.json` (`journalId: "journal-01"` for today's date). Visually confirmed via Playwright: the Journal card renders correctly (chip + prompt, styled identically to Anchor), zero console errors.

- **2026-07-13, careful operational audit of the daily-refresh pipeline (requested explicitly, "zero input from me"):** read all three workflow files in full rather than trusting memory of their last-known state. Findings:
  - **Mechanically correct, all three files:** `daily.yml`'s cron (`56 21 * * *` = 21:56 UTC = 05:56 HKT next day, a deliberate few-minute buffer before the 06:00 HKT boundary used by the staleness chip) and `watchdog.yml`'s cron (`0 1 * * *` = 01:00 UTC = 09:00 HKT, a ~3h buffer after the expected refresh) are both correct for the stated "6am HKT" goal. `daily.yml`'s permissions (`contents: write` for the commit/push, `actions: write` for the `gh workflow run pages-deploy.yml` dispatch step) are exactly what each step needs, no more. The v1.5 fix (bot-authored pushes don't trigger other workflows' `on: push` due to GitHub's own loop-prevention, so `daily.yml` must explicitly dispatch `pages-deploy.yml`) is still intact and unaffected by any of this session's later changes — confirmed by reading the actual current file, not assuming it survived.
  - **A live, end-to-end proof, not just a config read:** dispatched `daily.yml` for real against the just-pushed v1.12 code (commit `7eea4e5`). It correctly ran the rewritten `generate-daily.mjs`, detected a change (a fresh `generatedAtISO` always differs even when the picks repeat), committed `d158b18` with the new schema (`anchorId`/`journalId`/`wordId` — confirmed `journalId` renders correctly, not a leftover `shiftId`), and successfully dispatched `pages-deploy.yml`, which deployed. Curled the live site immediately after: `data/daily.json` matches the bot commit byte-for-byte, including `generatedAtISO`. Then dispatched `watchdog.yml`: passed clean (no stale-build issue filed), confirming its live-vs-committed comparison logic still works correctly against the new schema. Rebased local `main` onto the bot's commit per the standing convention. This proves the entire mechanism — generation, commit, deploy-chain dispatch, live propagation, and health-checking — works correctly end to end, not just "the YAML looks right."
  - **The one honest gap, stated plainly rather than glossed over:** every single run of `daily.yml` and `watchdog.yml` to date — before and including today's — has been a manual `workflow_dispatch`, never a real `schedule` trigger. Checked directly (`list_workflow_runs` filtered to `event:schedule` for both workflows): zero results, for both. This is not a defect; the workflows were only created a few hours ago today (`created_at` ~11:07/12:28 HKT per the Actions API), and daily.yml's first scheduled occurrence after that creation time is tonight (2026-07-13T21:56Z), with watchdog's first following at 2026-07-14T01:00Z — neither has happened yet as of this audit. Manual dispatch exercises the identical job body a cron trigger would (same steps, same permissions, same script) — the only untested variable is literally "does GitHub's scheduler fire the cron," a platform-reliability question, not something specific to this repo's configuration. I am not claiming more certainty than I actually have here.
  - **Closing the loop for real, not just asserting confidence:** scheduled a `send_later` check-in for 2026-07-14T01:20Z (after both workflows' first genuine scheduled fire) to actually confirm — via the same live-verification method used above (check run history for `event:schedule`, confirm the bot commit, confirm the live site, confirm watchdog passed) — that the unattended overnight run genuinely worked, rather than leaving this as an assumption. Will report back if anything is actually wrong; will stay quiet if it's clean.
  - **Other reliability notes, already documented, reconfirmed still accurate:** GitHub Actions cron can fire several minutes late under scheduler load (the 05:56/09:00 HKT slots were deliberately chosen off the top-of-hour congestion point, and watchdog's 3h buffer absorbs ordinary delay without a false alarm); scheduled workflows auto-disable after ~60 days of zero repo activity, not a near-term concern given how active this repo has been. One incremental reliability improvement worth noting: v1.10's removal of the RSS-fetching machinery from `generate-daily.mjs` means the daily job's realistic failure surface is now much smaller than it was through v1.9 — no network fetch, no feed parsing, no timeout to hit; the only remaining failure modes are a malformed `cards.json` or a `git push` race against a concurrent human push to `main` (a standard, inherent risk for any bot-plus-human-commits repo, not something introduced by this session's changes).

- **2026-07-13, v1.13 — Word of the Day title centered + italicized; Anchor logic explored (discussion only, no change):** two parts to the live feedback.
  - **Word of the Day styling (actioned):** "the actual word to be centred instead of sitting on the left. Make it italics too." Changed `.word-title-row` to add `justify-content: center` and `.word-title` to add `font-style: italic` + `text-align: center` — purely presentational, no markup/schema/rotation-logic change. `verify.mjs all` stayed 59/59 (nothing about content shape changed). Verified visually via Playwright at 390px width: "Gemütlichkeit" renders centered and italic with the 🔊 button beside it, meaning/origin unchanged below. Separately confirmed "the word changes daily" directly against the shipped rotation logic (not by assertion): ran `lib.mjs`'s real `pickIndex`/`hktDayNumber` against the real `data/cards.json` for 10 consecutive HKT dates and got 10 distinct words (Gemütlichkeit, Dharma, Mono no aware, Ren, Tsundoku, Ataraxia, Ubuntu, Ikigai, Saudade, Amor fati) — the deterministic-rotation guarantee (no repeats within a full `wordOfDay`-pool-sized cycle) holds as designed.
  - **Anchor logic (explored, explicitly not actioned per the human's own "let's explore this before making any changes"):** the human said the Anchor "doesn't feel like much of an anchor" and asked for the current logic explained before any change. Reviewed the actual `anchors` pool in `data/cards.json` (129 entries: `stoic:25, diewithzero:20, growth:20, relationships:15, wealth:20, focus:20, voices:9`) and sampled real entries per category to ground the analysis in shipped content, not assumption. Three concrete reasons offered for why it doesn't function as an anchor in practice:
    1. **Register mismatch across categories** — Stoic (Epictetus/Seneca/Marcus Aurelius, grounding/being-oriented) sits in the same rotation as Carnegie-style relationship tactics and Newport-style productivity advice; day to day there's no thematic throughline, so nothing accumulates.
    2. **Tactical-vs-grounding content mix** — a real share of the pool (especially Relationships/Focus, parts of Wealth/Die-With-Zero) reads as actionable instruction ("do X to get Y") rather than a principle to sit with and return to; an anchor is meant to be something you rest on, not another to-do.
    3. **Rotation cycle too long to "anchor" anything** — 129 entries means roughly one full cycle every ~4.3 months; no single idea recurs often enough within a normal span of days/weeks for the user to actually feel anchored *by* it, as opposed to just seeing a new quote each morning.
    - Offered three candidate directions, explicitly not chosen or implemented: (a) narrow the pool to timeless/being-oriented principles only, dropping the tactical entries; (b) keep the full pool but weight the rotation so a smaller core subset of grounding ideas recurs more often; (c) rewrite the tactical entries themselves toward observation/being rather than instruction, keeping the category breadth. Per the invariant-12 ratchet and the human's explicit "let's explore... before making any changes," **no changes were made to `anchors` content, `pickIndex`, or any Anchor-related code in v1.13** — awaiting the human's steer on which direction (or mix) to take before touching it.
  - **BUILD-PLAN.md** bumped to v1.13 with a changelog entry covering both parts (the shipped CSS change and the open Anchor discussion, explicitly marked as not yet acted on) and the §4.5 item 3 component description updated to reflect the centered/italic word title.

- **2026-07-13, v1.14 — Anchor pool fully replaced: 129 tactical/mixed-register entries → 365 observation-voice entries, one per day of the year:** the human picked a direction from the three offered in v1.13 — "rewrite the tactical ones toward observation/being rather than instruction... reframe close every tab as something more like noticing the pull of attention, not a to-do" — with calmness/mindfulness/grounding as the explicit center theme, plus expanding the pool to 365 and asking for deep research grounding from named figures (e.g. Obama, Shetty) and philosophies (e.g. Stoics), not guessed-at content.
  - **Taxonomy redesign:** rather than patch the old 7 categories (`stoic`, `diewithzero`, `growth`, `relationships`, `wealth`, `focus`, `voices`), the pool was redesigned around 10 categories chosen to give the observational voice real depth and breadth: `stoic` (55 — Marcus Aurelius, Seneca, Epictetus), `buddhist` (55 — Thich Nhat Hanh, Jon Kabat-Zinn, the Dalai Lama, Pema Chödrön; new category, since Buddhist teaching is one of the deepest wells of observation-over-instruction writing available and the old pool had none), `taoist` (25 — Lao Tzu, Zhuangzi; also new), `impermanence` (35, replacing `diewithzero` — Bill Perkins/Die With Zero reframed away from spend-your-points tactics toward noticing the assumption that there will always be more time later), `attention` (35, replacing `focus` — Cal Newport reframed away from productivity-hack instruction toward noticing the pull of distraction itself), `relationships` (30 — Dale Carnegie reframed away from social technique toward noticing habits of attention/inattention toward other people), `growth` (30 — Carol Dweck expanded with Kristin Neff and Brené Brown, reframed away from achievement-coaching toward noticing the inner critic with curiosity/compassion), `money` (25, replacing `wealth` — Morgan Housel reframed away from wealth-building tactics toward noticing comparison/status anxiety around money), `voices` (40, expanded from 3 named figures to 8 — Jay Shetty, Barack Obama, Michelle Obama, Nelson Mandela, Desmond Tutu, Fred Rogers, Viktor Frankl, Naval Ravikant), and `grounding` (35, entirely new — universal sensory/body observations with no named attribution at all, deliberately zero research/attribution risk, pure noticing of breath/body/senses). Counts were chosen so Stoic and Buddhist (the two richest sources of genuinely observational, equanimity-centered writing) anchor the pool at 110/365 combined, with `grounding` (35) as a risk-free foundation and `voices` (40) giving the named-figure breadth the human specifically asked for.
  - **12 parallel research-and-author passes**, one per category (splitting `stoic` into Marcus-Aurelius/Seneca-Epictetus and `buddhist` into Thich-Nhat-Hanh-Kabat-Zinn/Dalai-Lama-Chödrön for quality control at that batch size), each explicitly required to web-search its source figures' actual documented themes/books before writing (not work from vague memory of "stoic-sounding quotes"), and each given a hard voice mandate with worked before/after calibration examples drawn from this app's own retiring content, e.g.: OLD (instruction) "Depth is rare, so depth is valuable. Protect three uninterrupted hours the way you protect sleep" → NEW (observation) "Notice how quickly a quiet, unscheduled hour starts to feel suspicious, like something must be wrong. Nothing is wrong. Let it stay quiet a little longer." Agents were explicitly told not to open every entry with "Notice" (checked after the fact: only 1/365 did) and to keep every entry concrete and specific to a real recognizable moment rather than vague/platitudinous.
  - **Mechanical validation, first pass, 365/365 clean:** word cap ≤40 (actual range 19–40), zero quotation-mark glyphs, zero banned platitudes, zero exact-duplicate text, and zero near-duplicate text (>60% token overlap) across the *entire* 365-entry pool — a stronger bar than any previous content round, since previous rounds' near-dup checks only ran within one batch at a time.
  - **The real editorial work: 4 independent, fresh-context QA passes** (mirroring the 4-way authoring split so each reviewer saw a coherent group of categories), each fact-checking four to five things a mechanical script cannot catch: (1) attribution confidence — is this idea genuinely, centrally documented as this specific person's own theme, verified via web search, not a guess; (2) verbatim-quote-in-spirit risk — does a paraphrase track a person's single most-famous line too closely even with zero quote-mark glyphs; (3) voice compliance — does this actually read as a noticed moment, not an instruction; (4) quality/vagueness; and for the `voices` batch specifically, (5) strict political neutrality for Obama/Michelle Obama/Mandela/Tutu (no reference to office, party, policy, or elections, per the same extra bar §5.3.9 established in v1.3). These passes surfaced 68 genuine issues across the 365 — roughly 19%, in the same range as the original v1.0 build's QA yield (74/170, ~44%, for a harder first-time voice) and meaningfully more than a rubber-stamp review would find:
    - **A systemic voice relapse in `relationships`:** ~11 of 30 entries had been drafted with a strong first sentence (a genuine observation) followed by a "Notice X" or "Watch Y" command tail that quietly turned the whole thing back into an instruction — exactly the pattern this entire rewrite exists to eliminate. Caught and rewritten to end on the noticed thing itself, no directive tail.
    - **Quote-in-spirit risk across several categories:** Marcus Aurelius's "born to do the work of a human being" (Meditations 5.1) and "no man loses any other life than this which he now lives" lines; Seneca's "to be everywhere is to be nowhere" (Letter 2) and "it takes the whole of life to learn how to live"; Epictetus's own cup-breaking example (Enchiridion 3, the original draft literally used "a favorite mug" — swapped for "a phone screen"); two Thich Nhat Hanh entries both restating his signature cloud-in-the-paper interbeing parable (a same-batch duplicate as well as a quote risk — both replaced with distinct themes: habit energy, the mindfulness bell); his "I have arrived, I am home" gatha; two Lao Tzu entries both restating the Tao Te Ching's famous water-flows-to-low-ground image; Carol Dweck's branded "not yet" phrase (from her Power of Yet TED talk); Brené Brown's "shame cannot survive being spoken" and her guilt-vs-shame ("I did something bad" / "I am bad") formula; Desmond Tutu's ubuntu line ("a person is a person through other persons"); Naval Ravikant's "peace is happiness at rest" subtraction formula and his desire-as-a-contract line; and Morgan Housel's Man in the Car Paradox and "fee, not a fine" metaphor.
    - **A genuine misattribution caught and removed, not softened:** one Viktor Frankl entry was built on the "space between stimulus and response, in that space is our power to choose" idea — independent research (cross-checked against the Viktor Frankl Institute and Quote Investigator) confirms this is a well-documented misattribution; Frankl never wrote or said it. Rather than reword it to be less recognizable, the entry was dropped entirely and replaced with a fresh entry grounded in his actual, confirmed logotherapy work (meaning found through a task done carefully with no one checking).
    - **Political-neutrality fixes on 3 Obama entries:** an adversarial-listening framing that read as negotiation-with-political-opponents was replaced with his own well-documented, non-political marriage-counseling anecdote; "an hour of pickup basketball... back at the desk" had the office-adjacent "the desk" removed; "sustained criticism... over a season" (which read as approval-rating cycles) was replaced with his pre-political Chicago community-organizing history. One Tutu entry referencing Truth and Reconciliation Commission testimony (an official state body) was recentered on a personal, non-institutional instance of being heard.
    - **Three invented-scenario attribution-confidence fixes on Jay Shetty entries:** independent research found no documented anecdote behind a likes-counting scenario, a neighbor's-bags scenario, or a specific job-offer scenario attributed to him — each was rewritten to ground it directly in his actually-documented themes (validation vs. values, service/seva as an antidote to rumination, values vs. external validation) instead of a fabricated scene.
    - **A cluster of semantic near-duplicates the mechanical token-overlap proxy missed** (same cognitive move, different surface words — e.g. two `impermanence` entries both about a trip deferred until physical decline forecloses it, two `money` entries both about the goalpost moving before satisfaction arrives): each pair had one entry kept and the other replaced with a genuinely distinct observation, confirming the mechanical near-dup check is a real proxy but not a substitute for a reader actually comparing meaning, not just words.
    - **Four `growth` entries written as third-person research reportage** ("Research following students through repeated failure found...") rather than a first-person noticed moment — rewritten into the same close, present-tense observational voice as the rest of the pool.
  - **A dedicated fix pass** (same 4-way split) applied every flagged correction; verified programmatically (not just by the fixer's self-report) that every untouched entry in each batch stayed byte-identical to its pre-fix version, and that the corrected entries met the same mechanical rules. The reassembled 365-entry pool was diffed against the pre-fix drafts to confirm exactly 68 entries changed, matched 1:1 against the QA findings, before being written to `data/cards.json` with sequential `<category>-NNN` ids.
  - **`scripts/verify.mjs`:** `CATEGORY_COUNTS` and the total-anchors assertion retargeted from the old 7-category/129 shape to the new 10-category/365 shape — `verify.mjs all` stays 59/59 (a retarget of an existing check, not a net add or removal, consistent with the invariant-12 ratchet). `data/daily.json` was regenerated via the real `generate-daily.mjs` since its previously-committed `anchorId` (`rel-013`, from the now-retired `relationships`-with-old-ids scheme... actually from the old id namespace generally) no longer existed in the new pool; the regenerated file correctly picked `taoist-012` for today's date. Verified the rotation itself still works correctly post-replacement: ran the real `pickIndex`/`hktDayNumber` across 15 consecutive HKT dates and got 15 distinct anchor picks spanning most of the new categories. Verified visually via Playwright at 390×844: the Anchor card renders the new content correctly (chip, body, attribution), zero console errors.
  - **Full record:** `audits/CONTENT-REVIEW.md`'s old "Anchors, grouped by attribution" section (129 entries) is kept in full as a historical record, explicitly marked retired; a new "Anchors v1.14" section lists all 365 entries grouped by category, with all 68 QA-driven fixes flagged inline with their specific reasoning. `BUILD-PLAN.md` bumped to v1.14 with a full changelog entry covering the taxonomy redesign, the authoring/QA methodology, and representative findings.
